services:
    api:
        build: .
        ports:
          - published: 0
            target: $APACHE_PORT
    db:
        environment:
            POSTGRES_PASSWORD:
            POSTGRES_USER:
        image: postgres:17.0
